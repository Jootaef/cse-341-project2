# cse-341-project2
# 🧩 CSE 341 – Project 2: CRUD API with MongoDB

## 📌 Project Description

This is a Node.js and Express-based RESTful API for managing items in a MongoDB database. It includes full **CRUD (Create, Read, Update, Delete)** functionality, as well as **data validation** and **error handling**. The project is deployed using **Render** and prepared for future integration with **OAuth authentication**.

The project uses:
- Node.js
- Express
- MongoDB (Mongoose)
- CORS
- dotenv

---

## 🚀 How to Run Locally

1. **Clone the repository**
```bash
git clone https://github.com/Jootaef/cse-341-project2.git
cd cse-341-project2